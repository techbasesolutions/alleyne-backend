'use strict';

/**
 * property-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::property-submission.property-submission');
